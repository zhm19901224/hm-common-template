import React, { Component } from 'react';
import styles from './index.scss';

export default class TopBar extends Component {
  render() {
    return <header className={styles.tobBar}></header>;
  }
}
